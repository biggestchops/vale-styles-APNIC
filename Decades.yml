extends: substitution
message: "Use '%s' instead of '%s'. (Decades are plural, not possessive)."
level: error
ignorecase: true
# We wrap the entire regex in double quotes so YAML treats it as a single string key
swap:
  "(\\d{4})['’]s": "$1s"